export const environment = {
  production: true,
  /** Base URL for REST API (no trailing slash). Override per deployment. */
  apiUrl: '/api',
  /** Set false when a real backend serves `/history` at `apiUrl`. */
  useMockHistory: false,
};
